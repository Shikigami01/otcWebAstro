import { css } from '@emotion/react';
import { colors } from '../../utils/colors';

export const styles = {
  heading: css`
    font-size: 1.6rem;
    line-height: 1.5;
    font-weight: 700;
    border-bottom: 2px solid #040000;
  `,
};
