import { css } from '@emotion/react';
import { colors } from '../../utils/colors';

export const styles = {
  img: css`
    display: block;
    width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 0 2px 0 ${colors.black};
  `,
};
